# git config --global user.name "nombre":
 Configuracion inicial de git para agregar el nombre del usuario para luego mostrarlo en los commits.
# git config --global user.email "email": 
Identico al anterior pero configura el email del usuario.