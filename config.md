# git config --global user.name "nombre":
 Configuración inicial de git para agregar el nombre del usuario para luego mostrarlo en los commits.
# git config --global user.email "email": 
Idéntico al anterior pero configura el email del usuario.